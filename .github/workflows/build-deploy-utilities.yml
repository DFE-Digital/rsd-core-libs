name: CI & Pack GovUK.Dfe.CoreLibs.Utilities

on:
  push:
    branches: [ main ]
    paths:
      - "src/GovUK.Dfe.CoreLibs.Utilities/**"
  pull_request:
    branches: [ main ]
    paths:
      - "src/GovUK.Dfe.CoreLibs.Utilities/**"

jobs:
  build-and-test:
    uses: ./.github/workflows/build-test-template.yml
    with:
      project_name: GovUK.Dfe.CoreLibs.Utilities
      project_path: src/GovUK.Dfe.CoreLibs.Utilities
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  pack-and-release:
    needs: build-and-test
    if: needs.build-and-test.result == 'success'
    uses: ./.github/workflows/pack-template.yml
    with:
      project_name: GovUK.Dfe.CoreLibs.Utilities
      project_path: src/GovUK.Dfe.CoreLibs.Utilities/GovUK.Dfe.CoreLibs.Utilities.csproj
