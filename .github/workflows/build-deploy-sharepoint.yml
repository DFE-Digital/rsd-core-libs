name: CI & Pack GovUK.Dfe.CoreLibs.SharePoint

on:
  push:
    branches: [ main ]
    paths:
      - "src/GovUK.Dfe.CoreLibs.SharePoint/**"
  pull_request:
    branches: [ main ]
    paths:
      - "src/GovUK.Dfe.CoreLibs.SharePoint/**"

jobs:
  build-and-test:
    uses: ./.github/workflows/build-test-template.yml
    with:
      project_name: GovUK.Dfe.CoreLibs.SharePoint
      project_path: src/GovUK.Dfe.CoreLibs.SharePoint
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  pack-and-release:
    needs: build-and-test
    if: needs.build-and-test.result == 'success'
    uses: ./.github/workflows/pack-template.yml
    with:
      project_name: GovUK.Dfe.CoreLibs.SharePoint
      project_path: src/GovUK.Dfe.CoreLibs.SharePoint/GovUK.Dfe.CoreLibs.SharePoint.csproj
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
