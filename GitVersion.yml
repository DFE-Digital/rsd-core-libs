workflow: TrunkBased/preview1
tag-prefix: '^.*-'

branches:
  main:
    increment: Patch
    label: ''
    prevent-increment:
      of-merged-branch: true

  pull-request:
    increment: Patch
    source-branches: ['main']
    label: prerelease
    prevent-increment:
      of-merged-branch: true