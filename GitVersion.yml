mode: Mainline
tag-prefix: '^.*-'

branches:
  main:
    increment: Patch
    prevent-increment-of-merged-branch-version: true
    label: ''
  pull-request:
    increment: Patch
    source-branches:
      - main
    label: prerelease
